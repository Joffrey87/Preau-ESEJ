[build]
  command = "next build"
# Pas de `publish` : le runtime Next.js de Netlify gère lui-même la publication
# (rendu serveur, middleware, routes dynamiques, contenu statique).

[[plugins]]
  package = "@netlify/plugin-nextjs"
